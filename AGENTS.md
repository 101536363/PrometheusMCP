# 外部记忆系统

- 项目根目录创建 `.opencode-memory/` 目录
- `.opencode-memory/logs/`：持久化记录所有尝试、错误、解决方案
- `.opencode-memory/context.md`：当前任务上下文摘要（追加模式）
- `.opencode-memory/progress.md`：项目 todolist 进度清单

## 阶段1: 项目初始化（幂等性）

1. 检查目录结构 → 缺失的才创建，已有内容不覆盖
2. 检查 Git 仓库 → 未初始化的才 init
3. 检查 `.gitignore` → 缺失规则才追加（包含 .env, logs/, .opencode-memory/,其他一些常规略目录和文件）
4. 检查 `PROGRESS.md` → 未存在才创建
5. 检查 `README.md` → 未存在才创建
6. **核心原则**：已有内容绝不覆盖，只做补充

## 阶段2: 迭代开发循环

### 任务读取

- 从 `PROGRESS.md` 读取当前任务
- 任务完成前不开始下一个
- 每次读取后更新 `context.md`（追加模式）

### 代码编写

- 一次只做一个功能
- 先写测试用例（测试驱动）
- 保持代码简洁
- 所有函数需错误处理

### 测试验证

- 默认测试会失败
- 持续调试直到测试通过
- 记录所有失败原因到日志

### Git 提交

- 每个功能单独提交
- 提交信息格式：`[功能名] 描述`
- 失败尝试也提交（标注 WIP）

### 上下文管理

- 每轮结束后更新 `context.md`（追加新内容，不覆盖历史）
- 清理不必要的临时文件
- 保持工作目录干净

## 代码质量要求

- 所有函数需错误处理
- 敏感信息使用环境变量（禁止硬编码）
- 测试覆盖率 > 80%
- Lint/Typecheck 必须通过
- 禁止提交 secrets 到版本控制

# 日志规范

- 日志文件命名：`logs/YYYY-MM-DD.md`
- 每次错误记录：时间、错误内容、尝试的解决方案
- 每次成功记录：解决方案总结
- **追加到日志末尾**，不清理历史日志

# 行为约束

- 遵循最小变更原则，只做必要的修改
- 修改前先确认备份/记录原状态
- 重大变更需有测试覆盖
- 复杂业务逻辑需要中文注释
